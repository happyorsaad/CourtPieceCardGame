import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fr.games.courtpiece',
  appName: 'court-piece',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
